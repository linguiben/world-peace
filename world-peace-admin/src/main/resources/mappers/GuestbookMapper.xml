<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jupiter.admin.mapper.GuestbookMapper">

    <select id="listMessages" resultType="com.jupiter.admin.entity.WpGuestbookMessage">
        SELECT id, username, content, ip, created_at
        FROM wp_guestbook_message
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <insert id="insertMessage">
        INSERT INTO wp_guestbook_message (username, content, ip, created_at)
        VALUES (#{username}, #{content}, #{ip}, NOW())
    </insert>

</mapper>
